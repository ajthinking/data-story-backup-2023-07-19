import { Input } from "postcss";
import { expect, it, describe } from "vitest";
import { InputDevice } from "./InputDevice";

describe('pull', () => {
  it('returns items at port named "input"', async () => {
    const input = new InputDevice
  })

  it('removes the items pulled from the links', async () => {
    const input = new InputDevice
  })
})

describe('pullFrom', () => {
  it('returns items at named port', async () => {
    const input = new InputDevice
  })

  it('removes the items pulled from the links', async () => {
    const input = new InputDevice
  })  
})