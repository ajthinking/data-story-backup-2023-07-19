export const addNode = () => {
  // const newNode = {
  //   id: `node-${nodes.length + 1}`,
  //   type: "transformer",
  //   position: { x: 0, y: 0 },
  //   data: { label: `Node ${nodes.length + 1}` },
  // };
};